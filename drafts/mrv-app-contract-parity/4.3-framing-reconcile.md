# DRAFT — §4.3 framing reconcile (not published)

> **Status: DRAFT. Do not publish.** Proposed replacement for §4.3 "No EVM
> execution". The refusal is unchanged in substance; the wording is tightened so
> the "no EVM" claim is not read as "no EVM-shaped developer context" once the
> host-context syscalls (§14.2) are active.

---

## Current text (for reference)

> ### 4.3 No EVM execution
>
> Monolythium is **not EVM-compatible at execution**. Existing EVM bytecode does
> not run on the chain. Solidity is not the default developer model. The token,
> NFT, and account-abstraction conventions from the Ethereum ecosystem are not
> the chain's protocol standards.
>
> This is a deliberate position. The chain is **EVM-connected at the liquidity
> edge** — value moves in and out through light-client and zero-knowledge
> bridges, cross-chain swaps, and issuer-supported integrations — but value, once
> it arrives, settles through Mono-native standards on a Rust/RISC-V execution
> layer.
>
> The reasoning is in §9. ...

## Proposed text

> ### 4.3 No EVM bytecode execution
>
> Monolythium does **not execute EVM bytecode**. Compiled EVM contracts do not
> run on the chain, and Solidity is not the default developer model. The token,
> NFT, and account-abstraction conventions from the Ethereum ecosystem are not
> the chain's protocol standards.
>
> This is a deliberate position. The execution layer is Rust/RISC-V. What the
> chain does provide for application logic ported from other ecosystems is an
> **EVM-equivalent host context** — the familiar shape of block and call metadata
> (a committed block timestamp, a chain identifier, and the value attached to a
> call) exposed through the host syscall ABI in §14.2. This is a context shape,
> **not** bytecode-level compatibility: there is no EVM interpreter, no Solidity
> ABI at the protocol layer, and no implicit account-abstraction model. Ported
> logic is recompiled to a deterministic Mono RISC-V artifact; it is not run as
> EVM bytecode.
>
> The chain remains **EVM-connected at the liquidity edge** — value moves in and
> out through light-client and zero-knowledge bridges, cross-chain swaps, and
> issuer-supported integrations — but value, once it arrives, settles through
> Mono-native standards on the Rust/RISC-V execution layer.
>
> The reasoning is in §9. The short version: trying to be a faster Ethereum has
> been tried and produces well-funded chains that nevertheless live inside
> Ethereum's frame. The chain optimizes for the audience that wants Rust
> contracts, AI-assisted developer ergonomics, post-quantum accounts, native
> modules for hot paths, and a smaller and cleaner protocol surface.

## Why the change

The host-context syscalls in §14.2 (`block_timestamp`, `chain_id`,
`call_value`) give ported application logic the block/call metadata it expects.
A reader who sees those syscalls should not find a flat "no EVM-compatible
execution" sentence that reads as contradicting them. The revised wording draws
the line precisely where it actually is: **no EVM bytecode and no Solidity
protocol standards, but an EVM-equivalent host context for ported app logic.**

## Note on the section title

The heading changes from "No EVM execution" to "No EVM bytecode execution" so
the refusal stays unambiguous while leaving room for the host-context shape. If
the heading is kept verbatim for cross-reference stability, the first sentence
still carries the precise claim. Confirm against any anchors that link to this
section before changing the heading slug.

> Editorial note: this reconcile assumes the §14.2 host-context syscalls have
> shipped and activated. If the whitepaper revision is published before
> activation, mark the EVM-equivalent host context as a near-term addition rather
> than a present-tense capability, consistent with the rest of the document's
> honesty posture.
